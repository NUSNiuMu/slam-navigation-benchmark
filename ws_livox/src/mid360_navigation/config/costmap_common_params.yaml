obstacle_range: 3.5
raytrace_range: 4.0

footprint: [[-0.07, -0.1], [-0.07, 0.1], [0.07, 0.1], [0.07, -0.1]]

inflation_radius: 0.15
cost_scaling_factor: 2.0

# Explicit plugin namespace params (for plugin-based costmap configs)
obstacle_layer:
  enabled: true
  max_obstacle_height: 2.0
  observation_sources: scan
  scan:
    sensor_frame: base_link
    data_type: LaserScan
    topic: /scan
    marking: true
    clearing: true
    inf_is_valid: true
    expected_update_rate: 0
    observation_persistence: 0
    obstacle_range: 3.5
    raytrace_range: 4.0
